({
    doInit : function(component, event, helper) {
        helper.doInit(component, event, helper);
    },
    setCardColor : function(component, event, helper){
        helper.setCardColor(component, event, helper);
    }
})

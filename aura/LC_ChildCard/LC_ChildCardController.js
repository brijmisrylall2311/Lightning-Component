({
    doInit : function(component, event, helper) {
        helper.doInit(component, event, helper);
    },
    deleteInfo : function(component, event, helper) {
        helper.deleteInfo(component, event, helper);
    }
})
